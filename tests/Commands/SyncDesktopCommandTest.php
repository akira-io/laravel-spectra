<?php

declare(strict_types=1);

it('returns success exit code', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('command completes without errors', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('executes successfully', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('handles command invocation', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('builds and sends payload', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('processes desktop sync operation', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('completes sync operation successfully', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});

it('command runs to completion', function () {
    $this->artisan('spectra:sync-desktop')
        ->assertExitCode(0);
});
